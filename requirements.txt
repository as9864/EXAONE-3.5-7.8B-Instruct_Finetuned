# ---------------------------------------------------------------------------
# PyTorch는 GPU에 맞는 인덱스에서 먼저 설치하세요 (RTX 50xx = Blackwell sm_120 -> cu128 필요)
#
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
#
# 그 다음 이 파일을 설치합니다:  pip install -r requirements.txt
# ---------------------------------------------------------------------------

# EXAONE-3.5는 trust_remote_code 기반 커스텀 모델링 코드를 사용합니다.
# 너무 최신 transformers에서는 remote code가 깨질 수 있어 검증된 버전으로 고정합니다.
transformers==4.48.3
peft>=0.14.0
accelerate>=1.3.0
datasets>=3.2.0
bitsandbytes>=0.46.0     # 4-bit QLoRA (Windows wheel 포함 / sm_120은 0.46+ 필요)
safetensors>=0.4.5
sentencepiece>=0.2.0

# 학습 유틸
pyyaml>=6.0
tqdm>=4.66.0
numpy>=1.26.0

# 평가
rouge-score>=0.1.2

# 선택 사항 (필요할 때만)
# tensorboard>=2.18.0
# konlpy>=0.6.0          # 한국어 형태소 단위 ROUGE (JDK 필요)
# bert-score>=0.3.13
