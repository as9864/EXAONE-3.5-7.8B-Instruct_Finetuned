# ---------------------------------------------------------------------------
# PyTorch는 GPU에 맞는 인덱스에서 먼저 설치하세요 (RTX 50xx = Blackwell sm_120 -> cu128 필요)
#
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
#
# 그 다음 이 파일을 설치합니다:  pip install -r requirements.txt
# ---------------------------------------------------------------------------

# EXAONE-3.5는 trust_remote_code 기반 커스텀 모델링 코드를 사용합니다. 버전이 달라지면
# remote code가 조용히 깨지므로, 실제로 학습·추론을 완주한 버전으로 정확히 고정합니다.
#
# 5.15.0에서 QLoRA 학습 1에폭(1,199스텝) + 추론 + 서버까지 검증했습니다. 로딩 중
# `torch_dtype is deprecated` / `cache_position ... not documented` 경고가 뜨지만
# 동작에는 영향이 없습니다. (전역 5.9.0에서는 로딩 실패 — docs/WORKLOG.md D4 참고)
transformers==5.15.0
peft>=0.14.0
accelerate>=1.3.0
datasets>=3.2.0
bitsandbytes>=0.46.0     # 4-bit QLoRA (Windows wheel 포함 / sm_120은 0.46+ 필요)
safetensors>=0.4.5
sentencepiece>=0.2.0

# 학습 유틸
pyyaml>=6.0
tqdm>=4.66.0
numpy>=1.26.0

# 평가
rouge-score>=0.1.2

# 선택 사항 (필요할 때만)
# fastapi>=0.115         # HTTP 서버 (pip install -e .[serve] 로도 설치 가능)
# uvicorn[standard]>=0.30
# tensorboard>=2.18.0
# konlpy>=0.6.0          # 한국어 형태소 단위 ROUGE (JDK 필요)
# bert-score>=0.3.13
